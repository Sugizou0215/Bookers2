
<h1>Books</h1>
<% @books.each do |book| %>
<div>
  <%= link_to book_path(book.id) do %>
  <% end %>
  <span>
    <%= link_to book.title, book_path(book.id) %>
  </span>
  <p>body：<%= book.body %></p>
</div>
<% end %>